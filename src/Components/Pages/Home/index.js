import { useHistory, useLocation } from 'react-router-dom'

function Home(props) {

    return (
        <div className="home-root-container">
            Hello, World
        </div>
    )
}

export default Home